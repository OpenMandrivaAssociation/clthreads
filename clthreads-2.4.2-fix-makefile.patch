diff -rupN clthreads-2.4.2.old/source/Makefile clthreads-2.4.2/source/Makefile
--- clthreads-2.4.2.old/source/Makefile	2018-08-24 20:40:37.000000000 +0200
+++ clthreads-2.4.2/source/Makefile	2025-08-19 15:17:55.227997672 +0200
@@ -31,9 +31,8 @@ MINVERS = 4.2
 VERSION = $(MAJVERS).$(MINVERS)
 
 
-CPPFLAGS += -DVERSION=\"$(VERSION)\" -D_REENTRANT -D_POSIX_PTHREAD_SEMANTICS -I.  
+CPPFLAGS += -DVERSION=\"$(VERSION)\" -D_REENTRANT -D_POSIX_PTHREAD_SEMANTICS -I.
 CXXFLAGS += -Wall -O2 -fPIC
-CXXFLAGS += -march=native
 LDLFAGS += 
 LDLIBS +=
 
@@ -47,16 +46,17 @@ CLTHREADS_H = clthreads.h
 
 
 $(CLTHREADS_MIN): $(CLTHREADS_O)
-	$(CXX) -shared $(LDFLAGS) -Wl,-soname,$(CLTHREADS_MAJ) -o $(CLTHREADS_MIN) $(CLTHREADS_O) $(CLTHREADS_DEP)
+	$(CXX) $(LDFLAGS) -shared -Wl,-soname,$(CLTHREADS_MAJ) -o $(CLTHREADS_MIN) $(CLTHREADS_O) $(CLTHREADS_DEP)
 
 
 install:	$(CLTHREADS_MIN)
-	install -d $(DESTDIR)$(PREFIX)/$(INCDIR)
-	install -d $(DESTDIR)$(PREFIX)/$(LIBDIR)
+	install -d $(DESTDIR)/$(INCDIR)
+	install -d $(DESTDIR)/$(LIBDIR)
 	install -m 644 $(CLTHREADS_H) $(DESTDIR)$(INCDIR)
-	install -m 755 $(CLTHREADS_MIN) $(DESTDIR)$(LIBDIR)
-	ldconfig
-	ln -sf $(CLTHREADS_MIN) $(DESTDIR)$(LIBDIR)/$(CLTHREADS_SO)
+	install -m 755 $(CLTHREADS_MIN) $(DESTDIR)/$(LIBDIR)
+	
+	ln -sf $(CLTHREADS_MIN) $(DESTDIR)/$(LIBDIR)/$(CLTHREADS_MAJ)
+	ln -sf $(CLTHREADS_MIN) $(DESTDIR)/$(LIBDIR)/$(CLTHREADS_SO)
 
 uninstall:
 	rm -rf $(DESTDIR)$(INCDIR)/$(CLTHREADS_H)
